{
  "name": "brandie-chat-proxy",
  "version": "1.0.0",
  "description": "Simple proxy to relay chatbot requests to OpenAI (use OPENAI_API_KEY env var)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-fetch": "^2.6.12"
  }
}
